<project>
  <!-- Other configurations (modelVersion, groupId, artifactId, version) -->

  <!-- Add this parent section -->
  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.3.2</version> <!-- Use your Spring Boot version -->
    <relativePath/> <!-- Lookup parent from repository -->
  </parent>

  <dependencies>
    <!-- Other dependencies -->

    <!-- No need for <version> here if parent is set -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>

    <!-- Testcontainers dependencies (versions can be managed via parent/BOM) -->
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>testcontainers</artifactId>
      <version>1.16.3</version> <!-- Version required if no BOM -->
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>mysql</artifactId>
      <version>1.16.3</version>
      <scope>test</scope>
    </dependency>
  </dependencies>
</project>
